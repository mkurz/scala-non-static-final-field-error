GET      /*param1                                                 controllers.HomeController.foo(param1: String)

# Workaround by adding a second route
GET     /just-a-second-route                                      controllers.HomeController.bar()
